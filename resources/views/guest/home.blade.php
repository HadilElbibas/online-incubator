<html>
    <head>
      @extends('layouts.master')
    </head>
      {{-- <body>
      @section('content')
      @foreach($news as $newsitem)
            <img src="{{ asset($newsitem->image ) }}" />
            {{url($newsitem->title)}}
            {{url($newsitem->description)}}
          
    @endforeach
    @endsection
        </body> --}}
